<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.wildan.favoritemodule">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="com.wildan.moviecatalogue.READ_DATABASE"/>

    <uses-permission android:name="com.wildan.moviecatalogue.WRITE_DATABASE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_favorite_movie"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_favorite_movie_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>