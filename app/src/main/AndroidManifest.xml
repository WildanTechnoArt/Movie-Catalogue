<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.wildan.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <permission
            android:name="com.wildan.moviecatalogue.READ_DATABASE"
            android:protectionLevel="normal"/>
    <permission
            android:name="com.wildan.moviecatalogue.WRITE_DATABASE"
            android:protectionLevel="normal"/>

    <application
            android:allowBackup="true"
            android:hardwareAccelerated="true"
            android:icon="@mipmap/ic_mc_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_mc_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.SettingsActivity">
        </activity>

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".widget.FavoriteWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/favorite_widget_info"/>
        </receiver>

        <activity android:name=".activity.DetailMovieActivity"/>
        <activity android:name=".activity.DetailTvShowActivity"/>

        <provider
                android:name=".provider.FavoriteMovieProvider"
                android:authorities="com.wildan.moviecatalogue"
                android:exported="true"
                android:readPermission="com.wildan.moviecatalogue.READ_DATABASE"
                android:writePermission="com.wildan.moviecatalogue.WRITE_DATABASE"/>

        <receiver
                android:name=".service.DailyAlarmReceiver"
                android:enabled="true"
                android:exported="false">
        </receiver>

        <receiver
                android:name=".service.UpcomingAlarmReceiver"
                android:enabled="true"
                android:exported="false"/>

        <service
                android:name=".service.StackWidgetService"
                android:permission="android.permission.BIND_REMOTEVIEWS"/>
    </application>

</manifest>